package com.example.assistantapp.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val Shapes = Shapes(
    small = RoundedCornerShape(4.dp),  // Used for small components like buttons
    medium = RoundedCornerShape(8.dp), // Used for medium components like cards
    large = RoundedCornerShape(16.dp)  // Used for larger components like dialogs
)
